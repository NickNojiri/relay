{
  "name": "@relay/flag-node",
  "version": "0.1.0",
  "private": true,
  "description": "Native Node addon for the Relay flag engine (napi-rs over flag-core).",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "binaryName": "flag-node"
  },
  "scripts": {
    "build:napi": "napi build --platform --release",
    "build:napi:debug": "napi build --platform"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.0.4"
  }
}
